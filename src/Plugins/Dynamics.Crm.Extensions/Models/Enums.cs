namespace Dynamics.Crm.Models
{
    public enum LogEntryType
    {
        Trace = 0,
        Warning = 1,
        Error = 2,
        Info = 3,
    }

    public enum StateCode
    {
        Active = 0, Inactive = 1
    }

    public enum StatusCode
    {
        Active = 1, Inactive = 2
    }
}
